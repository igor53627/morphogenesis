# Morphogen RPC Adapter (prod)
UPSTREAM_RPC_URL=https://your-upstream-rpc.example

# Optional host bind for published JSON-RPC port.
# Default is 127.0.0.1 in compose for safer host exposure.
RPC_BIND_HOST=127.0.0.1

# Required dictionary/CAS endpoints.
# For local mode with --profile local-code-data use:
#   DICT_URL=http://code-data/mainnet_compact.dict
#   CAS_URL=http://code-data/cas
DICT_URL=https://your-dictionary-endpoint.example/mainnet_compact.dict
CAS_URL=https://your-cas-endpoint.example/cas

# Keep fail-closed in prod by default.
# If you intentionally enable degraded fallback, pair both flags:
#   --fallback-to-upstream --allow-privacy-degraded-fallback
